// Fill these in after creating your Supabase project (Project Settings -> API).
// The anon/public key is designed by Supabase to be safe in public client code -
// it only grants what your Row Level Security policies allow (see backend/schema.sql,
// which for this demo allows full open read/write - fine for synthetic demo data,
// not something to reuse for a tool holding real confidential data).
export const SUPABASE_URL = "https://YOUR-PROJECT-REF.supabase.co";
export const SUPABASE_ANON_KEY = "YOUR-ANON-PUBLIC-KEY";
